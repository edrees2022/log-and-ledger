# 🚨 الحل الفوري - أضف SHA-1 الصحيح

## ❌ المشكلة:
Firebase Console يحتوي على SHA-1 خاطئ!

**SHA-1 الحالي في Firebase (خاطئ):**
```
d1:5e:25:f4:31:84:b9:94:e6:d0:07:e4:71:b5:a7:a2:ff:49:af:43
```

**SHA-1 الصحيح الذي يجب إضافته:**
```
56:6F:90:14:87:45:7B:33:60:FD:28:14:B8:9F:4E:BA:5C:EE:10:78
```

**SHA-256 الصحيح:**
```
05:49:11:30:1C:8C:16:1B:AD:AA:7E:2C:5D:FF:13:BE:85:6E:BE:4D:1F:80:9A:BA:63:E6:FA:06:70:03:43:86
```

---

## ✅ الحل (دقيقتان):

### الخطوة 1: في الصفحة المفتوحة أمامك
أنت الآن في صفحة تطبيق Android الصحيحة، اتبع:

1. **مرّر للأسفل** إلى قسم **"SHA certificate fingerprints"**
   (أنت تراه الآن في الصورة)

2. **اضغط على زر "Add fingerprint"** (الموجود تحت SHA الحالي)

3. **الصق هذا SHA-1 بالضبط:**
   ```
   56:6F:90:14:87:45:7B:33:60:FD:28:14:B8:9F:4E:BA:5C:EE:10:78
   ```

4. **اضغط "Save"**

5. **اضغط "Add fingerprint"** مرة أخرى وأضف SHA-256:
   ```
   05:49:11:30:1C:8C:16:1B:AD:AA:7E:2C:5D:FF:13:BE:85:6E:BE:4D:1F:80:9A:BA:63:E6:FA:06:70:03:43:86
   ```

6. **اضغط "Save"**

### الخطوة 2: حمّل google-services.json الجديد

بعد إضافة SHA الصحيح:

1. **مرّر للأعلى** في نفس الصفحة
2. **اضغط على زر "google-services.json"** (تحت "SDK setup and configuration")
3. سيتم تحميل الملف الجديد

### الخطوة 3: استبدل الملف

في Terminal، اكتب:
```bash
cd "/Users/omar.matouki/TibrCode Apps/log_and_ledger_main"

# احفظ نسخة احتياطية
cp android/app/google-services.json android/app/google-services.json.old2

# انسخ الملف الجديد من Downloads
cp ~/Downloads/google-services.json android/app/

# تحقق من المحتوى
cat android/app/google-services.json | grep -A 2 certificate_hash
```

**يجب أن ترى:**
```json
"certificate_hash": "566f901487457b3360fd28148b9f4eba5cee1078"
```

### الخطوة 4: أعد بناء APK

```bash
npx cap sync android
cd android && ./gradlew assembleDebug
cd ..
cp android/app/build/outputs/apk/debug/app-debug.apk .
```

---

## 🎯 لماذا لم يعمل من قبل؟

- SHA-1 الموجود في Firebase: `d1:5e:25:f4:31:...` (خاطئ)
- SHA-1 الحقيقي للتطبيق: `56:6F:90:14:87:...` (الصحيح)
- عندما يحاول Google التحقق من التطبيق، يجد SHA-1 غير مطابق → يرفض تسجيل الدخول

---

## ✨ بعد هذه الخطوات:

1. ✅ ثبّت APK الجديد على الهاتف
2. ✅ افتح التطبيق
3. ✅ اضغط "Sign in with Google"
4. ✅ **سيعمل 100%!** 🎉

---

**ملاحظة مهمة:** يمكنك الاحتفاظ بـ SHA-1 القديم في Firebase وإضافة الجديد. Firebase يدعم عدة SHA لنفس التطبيق.
